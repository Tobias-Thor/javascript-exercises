/* Arrayer */

/* 1. Förklara vad en array är och vilka datatyper den kan bestå av. */

/* 2. Gör en array som innehåller 5 st olika frukter av datatypen string. */
let fruits = ['pears', 'apples', 'oranges', 'bananas', 'mangos'];
console.log(fruits);

/* 3. Gör en array som innehåller 3 olika datatyper, ex. string, number, array. */
// Hey, how cool, an array in an array.
let myArray = ['peas', 'broccoli', 'cale', 'cabbage', 'bell pepper', [5, 9, 11, 12, 77]];
console.log(myArray);

/* let animals = ['cat', 'hamster', 'parrot', 'funky chihuahua'] 
4. console.log() hur många värden arrayen ovan innehåller. */

/* 5. I arrayen ovan, hämta cat. */

/* 6. I arrayen ovan, hämta funky chihuahua. */

/* 7. I arrayen ovan, byt ut hamster mot tiger. */

/* 💡Level-up
let a = [1,2,3,7,8,9];
let b = [4,5,6];

Merga ner array b på index 3 i array a.
let arr = ['a', 'b', 'c'];
Klona ovanstående array.
Tips: Kolla på array-metoder */